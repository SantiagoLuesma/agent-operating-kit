# Example feature (placeholder)

This directory is a **structural example**, not an approved product feature.

Copy forms from `docs/features/_templates/` when creating a real feature:

```text
docs/features/<feature-name>/
  spec.md
  test-plan.md
  open-questions.md
  review.md          # after review-feature
  technical-design.md # when needed
  ux.md               # when needed
```

Do not implement this example. Replace or delete it in project instances.
