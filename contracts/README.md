# Contracts

Place public, versioned interface definitions here when a project instance
has them, for example:

- OpenAPI / AsyncAPI / GraphQL schemas
- event contracts
- shared error catalogs

This kit ships without application contracts. Empty means *not decided*, not
*no rules apply*.
